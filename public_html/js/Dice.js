function rollD2(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 2 + 1);
    }
	return result;
}

function rollD3(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 3 + 1);
    }
	return result;
}

function rollD4(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 4 + 1);
    }
	return result;
}

function rollD6(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 6 + 1);
    }
	return result;
}

function rollD8(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 8 + 1);
    }
	return result;
}

function rollD10(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 10 + 1);
    }
	return result;
}

function rollD12(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 12 + 1);
    }
	return result;
}

function rollD20(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 20 + 1);
    }
	return result;
}

function rollD100(numRolls) {
    result = 0;
    for (i = 0; i < numRolls; i++) {
        result += Math.floor(Math.random() * 100 + 1);
    }
	return result;
}